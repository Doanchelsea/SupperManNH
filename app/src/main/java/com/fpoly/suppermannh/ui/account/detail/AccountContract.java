package com.fpoly.suppermannh.ui.account.detail;

public interface AccountContract {
    void ShowSuccuer(String name,String images);
    void ShowError(int error);
    void ShowLoading(boolean show);
}
